services:
  frontend:
    build:
      context: .
    env_file:
      - ./.env
    environment:
      PORT: "3000"
      HOST: "0.0.0.0"
      OSEM_API_URL: "https://api.opensensemap.org/"
    ports:
      - "3000:3000"
